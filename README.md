# WH-347 Builder

Static web app that turns a QBO pay-stub PDF (or pasted text) + SAM.gov wage determination into a config JSON ready to feed into `build_wh347.py` for federal Davis-Bacon WH-347 certified payroll.

Open `index.html` in a browser. No server, no build step — pure static site.

Deployed via Cloudflare Pages.